<p>Here are shown <i>some interactive data visualization demos</i> that I've built with examples found on the web, which sometimes I've slightly modified with ideas from other examples. The sources are always available. At this point, the main data visualization library is: <a href="http://d3js.org/">D3.js</a></p>
<h2>List:</h1>
<div class="row">
    <ul class="list-group col-lg-4 col-lg-offset-1">
    <% demos.forEach(function(demo, index){ %>
        <li class="list-group-item"><%= index + 1%>.- <a href="<%= demo.route %>"><%= demo.name %></a></li>
    <% }); %>
    </ul>
</div>