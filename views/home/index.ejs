<% 
    var numberPerGroup = Math.ceil(demos.length/2);
    var groupedDemos = [];
    groupedDemos[0] = demos.slice(0, numberPerGroup);
    groupedDemos[1] = demos.slice(numberPerGroup, demos.length);
%>

<div class="bs-callout bs-callout-info">Here there are some <i>interactive data visualization demos</i> that are built with examples found on the web, which I've rewritten to CoffeeScript, and sometimes modified with ideas from other examples. The sources are always available. The idea is to cook some code using libraries like D3.js or Raphaël, and extensions of those.</div>

<div id="demos-legend">
  <ul class="bs-callout bs-callout-warning">
    <li><span class="bullet d3js">&#8226;</span> D3js</li>
    <li><span class="bullet raphael">&#8226;</span> Raphaël</li>
  </ul>
</div>

<div class="row" id="demos-lists">
    <div class="col-lg-offset-2 col-lg-3">
        <%- partial('_demos_list', {demos: groupedDemos[0], indexOffset: 0}) %>
    </div>
    <div class="col-lg-offset-2 col-lg-3">
        <%- partial('_demos_list', {demos: groupedDemos[1], indexOffset: numberPerGroup}) %>
    </div>
</div>